version: v2
modules:
  - path: proto # On dit à Buf que la racine des packages est le dossier 'proto'
lint:
  use:
    - STANDARD # Remplace DEFAULT (règle l'avertissement de dépréciation)
breaking:
  use:
    - FILE